#name: Android Debug Release
#
#on:
#  push:
#    tags:
#      - 'v*'           # 打 v1.0.0 这种 tag 时触发
#  workflow_dispatch:   # 也可以手动触发
#
#permissions:
#  contents: write      # 创建 GitHub Release 需要
#
#jobs:
#  build-debug:
#    runs-on: ubuntu-latest
#
#    outputs:
#      tag_name: ${{ steps.meta.outputs.tag_name }}
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v4
#        with:
#          distribution: temurin
#          java-version: 17
#          cache: gradle
#
#      - name: Grant gradlew permission
#        run: chmod +x ./gradlew
#
#      # 这里还是打 debug 包
#      # 以后想切 release 包，把 assembleDebug 改成 assembleRelease
#      - name: Build debug APK
#        run: ./gradlew assembleDebug
#
#      - name: Resolve tag
#        id: meta
#        run: echo "tag_name=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
#
#      - name: Upload build artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: app-debug-apk
#          path: app/build/outputs/apk/debug/*.apk
#          if-no-files-found: error
#
#  publish:
#    needs: build-debug
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Download build artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: app-debug-apk
#          path: release-files
#
#      # 自动建 GitHub Release，并把 APK 挂上去
#      # 以后想自写说明，可把 generate_release_notes 换成 body / body_path
#      - name: Create GitHub Release
#        uses: softprops/action-gh-release@v2
#        with:
#          tag_name: ${{ needs.build-debug.outputs.tag_name }}
#          generate_release_notes: true
#          files: release-files/*.apk